<card id="{{image.id}}">
  <preview class="overlay-trigger"><img src="{{image.url}}" class="{{image.orientation}} hvr-bounce-in" data-image-index="{{loop.index0}}"/></preview>
  <description>{{image.note|markdown}}</description>
</card>
